__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
# never commit the uncompressed bundled dataset (the .gz IS committed);
# audit F25: the old blanket data_cache/*.csv silently gitignored the
# sector map and indicator caches that fresh clones depend on
data_cache/all_stocks_5yr.csv
data_cache/Combined_News_DJIA.csv
data_cache/live/.lock
experiments/*/scores_*.csv.gz
data_cache/live/backfill.log
data_cache/live/backfill*.log
data_cache/xbrl/

# transient atomic-write artifacts
*.tmp
